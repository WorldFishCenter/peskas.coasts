% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fao-helpers.R
\name{compound_re_catch}
\alias{compound_re_catch}
\title{Compound relative error of total monthly catch (FAO toolkit p. 9)}
\usage{
compound_re_catch(cpue_vec, days_vec, F_total, alpha = 0.1)
}
\arguments{
\item{cpue_vec}{Numeric vector of trip-level CPUE observations.}

\item{days_vec}{Numeric vector of per-unit fishing-days observations
(one per active fishing unit in the period).}

\item{F_total}{Total number of fishing units in the frame.}

\item{alpha}{Significance level. Default 0.10.}
}
\value{
Single numeric value (proportion).
}
\description{
Implements the 7-step procedure that propagates uncertainty from both
fishing days and CPUE into the total-catch estimate:
}
\details{
\enumerate{
  \item CL_days  = t * s_days  / sqrt(n_days)
  \item CL_cpue  = t * s_cpue  / sqrt(n_cpue)
  \item max_days = mean_days + CL_days
  \item max_cpue = mean_cpue + CL_cpue
  \item max_catch = F * max_days * max_cpue
  \item CL_catch  = max_catch - (F * mean_days * mean_cpue)
  \item RE_catch  = CL_catch / mean_catch
}
}
