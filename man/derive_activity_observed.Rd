% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-fao.R
\name{derive_activity_observed}
\alias{derive_activity_observed}
\title{Fall-back activity coefficient derived from observed catch records}
\usage{
derive_activity_observed(
  landings,
  group_by,
  boat_col = "boat_name",
  date_col = "landing_date",
  days_in_period = 30L
)
}
\arguments{
\item{landings}{Trip-level data with one row per landing.}

\item{group_by}{Grouping columns (typically minor stratum × fu × period).}

\item{boat_col}{Column with unique boat identifier. Default "boat_name".}

\item{date_col}{Column with landing date. Default "landing_date".}

\item{days_in_period}{Either an integer scalar (e.g. 30) or a column name
giving the calendar denominator per group.}
}
\value{
Tibble with `ac_observed` and `n_unique_boats` per group.
}
\description{
When no separate effort survey exists, approximate AC as the fraction of
calendar days in the period on which a unique boat was observed landing.
This is biased upward (only observed units enter) and downward (only
sampled days). Use as a placeholder until BAC/PAB data are available.
}
\keyword{internal}
