% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-stock-assessment.R
\name{assess_species_stock}
\alias{assess_species_stock}
\title{Assess One Species with the Requested Methods}
\usage{
assess_species_stock(
  landings,
  scientific_name,
  priors,
  methods = c("lbi", "lbspr", "lbb"),
  lbb_engine = NULL
)
}
\arguments{
\item{landings}{A landing-feed tibble for a single country.}

\item{scientific_name}{A single scientific name.}

\item{priors}{A one-row life-history tibble for that species.}

\item{methods}{Character vector of methods to run; any of `"lbi"`,
`"lbspr"`, `"lbb"`. Default all three.}

\item{lbb_engine}{Optional LBB fitting routine (see [assess_stock_lbb()]).}
}
\value{
A tibble with one row per method, carrying the estimate columns of
  each method plus `scientific_name`, `n_measured`, `frac_measured` and
  `quality`.
}
\description{
Runs the length coverage summary, life-history priors and the chosen
assessment methods for a single species, and attaches the quality flag. This
is the per-species unit the orchestrator maps over.
}
\keyword{modeling}
\keyword{workflow}
