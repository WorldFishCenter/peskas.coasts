% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fao-estimation.R
\name{compute_trip_cpue}
\alias{compute_trip_cpue}
\title{Collapse a landings table to one row per trip}
\usage{
compute_trip_cpue(
  landings,
  trip_id_col = "submission_id",
  duration_col = "trip_duration",
  duration_units = c("hours", "days"),
  catch_col = "catch_kg",
  keep_cols = c("gaul_2_name", "landing_site", "fishing_unit", "landing_date")
)
}
\arguments{
\item{landings}{Validated landings with one row per species per trip.}

\item{trip_id_col}{Column with the trip identifier. Default
"submission_id".}

\item{duration_col}{Column with trip duration. Default "trip_duration".}

\item{duration_units}{Units of `duration_col`: "hours" (default) or "days".}

\item{catch_col}{Per-row catch in kg. Default "catch_kg".}

\item{keep_cols}{Columns to carry through unchanged (one value per
trip — typically stratum + fu identifiers + date).}
}
\value{
Tibble: one row per trip with `total_catch_kg`, `fishing_days`,
        `cpue_kg_day` and the `keep_cols` carried through.
}
\description{
Combines per-species rows into a single trip-level record with total
catch, then computes daily CPUE = total_catch / trip_duration_days.
Trips with non-positive or missing duration are dropped (FAO requires
a defined daily catch).
}
