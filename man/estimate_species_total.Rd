% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate-fao.R
\name{estimate_species_total}
\alias{estimate_species_total}
\title{Distribute total catch across species using observed proportions}
\usage{
estimate_species_total(
  landings,
  minor_estimates,
  join_keys = c("gaul_2_name", "landing_site", "fishing_unit", "year_month"),
  species_col = "catch_taxon",
  catch_col = "catch_kg"
)
}
\arguments{
\item{landings}{Validated landings (one row per species per trip).}

\item{minor_estimates}{Output of `estimate_minor_total()`.}

\item{join_keys}{Same keys used at the minor-stratum level.}

\item{species_col}{Species/taxon column. Default "catch_taxon".}

\item{catch_col}{Per-row catch in kg. Default "catch_kg".}
}
\value{
Tibble: one row per (group × species) with `total_catch_kg_species`.
}
\description{
FAO toolkit p. 7:
  Total_catch_fu_species = Total_catch_fu × Proportion_fu_species
}
\details{
Proportions are computed from observed species-level catch in the same
(minor stratum × fishing unit × period) group, normalised to sum to 1.
}
