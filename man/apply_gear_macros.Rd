% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fao-frame.R
\name{apply_gear_macros}
\alias{apply_gear_macros}
\title{Apply gear macro-categories to a landings or frame tibble}
\usage{
apply_gear_macros(data, gear_lookup = default_gear_lookup, gear_col = "gear")
}
\arguments{
\item{data}{A tibble containing the gear-name column.}

\item{gear_lookup}{A tibble with columns `gear` and `gear_class`. Default
`default_gear_lookup`.}

\item{gear_col}{Name of the column to remap. Default `"gear"`.}
}
\value{
The input tibble with `gear_col` overwritten by the macro-category.
}
\description{
Overwrites the gear-name column with its corresponding macro-category
from `gear_lookup`. Operates on a `gear` column by default, but the
column name is configurable (e.g. `standard_name` for the Airtable frame).
}
\details{
Rows where the gear value has no match in `gear_lookup` keep the original
label (a warning is emitted listing the unmapped values).
}
