% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-stock-assessment.R
\name{run_stock_assessment}
\alias{run_stock_assessment}
\title{Run the Stock-Assessment Framework}
\usage{
run_stock_assessment(
  n_species = 5L,
  countries = c("kenya", "mozambique", "zanzibar"),
  methods = c("lbi", "lbspr", "lbb"),
  start_date = "2024-01-01",
  lbb_engine = NULL,
  log_threshold = logger::DEBUG,
  package = "coasts"
)
}
\arguments{
\item{n_species}{Integer. Priority species per country. Default 5.}

\item{countries}{Character vector of country keys under `conf$api$trips`.
Default Kenya, Mozambique, Zanzibar.}

\item{methods}{Character vector of methods (`"lbi"`, `"lbspr"`, `"lbb"`).}

\item{start_date}{Earliest landing date to include, aligning with the
PDS-covered window. Default `"2024-01-01"`.}

\item{lbb_engine}{Optional LBB fitting routine (see [assess_stock_lbb()]).}

\item{log_threshold}{The logging threshold. Default `logger::DEBUG`.}

\item{package}{Name of the package whose `inst/conf.yml` to read. Default
`"coasts"`.}
}
\value{
Invisibly, a tibble of per-species, per-method assessments with
  quality flags.
}
\description{
Workflow orchestrator. For each country it downloads the validated landing
feed, selects the top economically important species, pulls life-history
priors, runs the length-based assessment methods with a data-quality flag on
each, and uploads a versioned parquet of the results.
}
\keyword{modeling}
\keyword{workflow}
