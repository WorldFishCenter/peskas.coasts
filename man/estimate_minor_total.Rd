% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fao-estimation.R
\name{estimate_minor_total}
\alias{estimate_minor_total}
\title{FAO total monthly catch per (minor stratum × fishing unit × period)}
\usage{
estimate_minor_total(
  cpue_summary,
  days_summary,
  frame,
  trips,
  join_keys = c("gaul_2_name", "landing_site", "fishing_unit", "year_month"),
  boat_col = "boat_name",
  date_col = "landing_date",
  alpha = 0.1
)
}
\arguments{
\item{cpue_summary}{Output of `summarize_cpue()`.}

\item{days_summary}{Output of `summarize_fishing_days()`.}

\item{frame}{Tibble with `F_total` per group.}

\item{trips}{Trip-level table (needed for the compound RE that
uses the raw vectors, not just summary stats).}

\item{join_keys}{Columns to join the summaries on. Default minor
stratum × fu × period.}

\item{boat_col}{Vessel-identifier column in `trips`. If `NULL` or
absent, the days-vector for compound RE collapses
to one observation per trip and the compound RE
degrades to the CPUE RE (with a warning).}

\item{date_col}{Date column in `trips`. Default "landing_date".}

\item{alpha}{For compound RE. Default 0.10.}
}
\value{
Tibble: one row per (minor stratum × fu × period) with
        total_catch, RE, and component diagnostics.
}
\description{
Implements the master FAO formula
  Total_catch = F × avg_fishing_days × avg_CPUE
with compound relative error per the 7-step procedure on toolkit p. 9.
}
